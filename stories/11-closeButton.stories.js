import React from 'react';
import closeButton from '../Components/closeButton';
import App from '../App';

export default {
    title: 'CloseButton'
};

export const closeButtons = () => { return App(closeButton) }