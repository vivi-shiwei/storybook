import React from 'react';
import alertDialog from '../Components/alertDialog';
import App from '../App';

export default {
    title: 'AlertDialog'
};

export const alertDialogs = () => { return App(alertDialog) }