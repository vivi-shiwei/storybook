import React from 'react';
import formControl from '../Components/formControl';
import App from '../App';

export default {
    title: 'FormControl'
};

export const formControls = () => { return App(formControl) }