import React from 'react';
import iconButton from '../Components/iconButton';
import App from '../App';

export default {
    title: 'IconButton'
};

export const iconButtons = () => { return App(iconButton) }