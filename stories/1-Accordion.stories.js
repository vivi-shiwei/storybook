import React from 'react';
import accordion from '../Components/accordion';
import App from '../App';

export default {
    title: 'Accordion'
};

export const accordions = () => { return App(accordion) }